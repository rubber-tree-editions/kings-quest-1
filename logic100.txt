#include "vars.txt"

LOGIC__HEADER



v97--;
if (v97 == 1) {
  v97 = 0;
  sound(2, f148);
  if (vSCORE < 50) {
    print(null);
    goto(LABEL41);
  }
  if (vSCORE < 100) {
    print("We, at Sierra, wish to thank you for playing King's Quest. We are pleased to see that you are doing so well and hope that your score will keep improving.");
    goto(LABEL41);
  }
  print("We, at Sierra, wish to thank you for playing King's Quest. We are delighted by your score and hope you are enjoying the game.");
}
LABEL41:
  if (f75) {
    normal.motion(o13);
    stop.motion(o13);
    stop.cycling(o13);
  }
LABEL53:
  if (said("quit") || controller(c6)) {
    stop.sound();
    quit(0);
  }
LABEL68:
  if (said("restore", "game") || controller(c2)) {
    stop.sound();
    restore.game();
  }
LABEL84:
  if (said("restart", "game") || controller(c3)) {
    stop.sound();
    restart.game();
  }
LABEL100:
  if (controller(c4)) {
    stop.sound();
    status();
  }
LABEL108:
  if (controller(c18)) {
    call(92);
  }
LABEL116:
  if (controller(c17)) {
    toggle(fSOUND_ENABLED);
  }
LABEL124:
  if (controller(c12)) {
    cancel.line();
  }
LABEL131:
  if (controller(c19)) {
    print();
  }
LABEL139:
  if (controller(c13)) {
    echo.line();
  }
LABEL146:
  if (controller(c14)) {
    pause();
  }
LABEL153:
  if (!fSAID_ACCEPTED_USER_INPUT) {
    if (said("anyword", "anyword") || said("anyword") || said("anyword", "anyword", "anyword")) {
      stop.sound();
      if (vROOM_NO == 65) {
        print("As you will never get out of this cage alive, I can only permit you to: Restore Game, Restart Game or Take Inventory. Sorry.");
        goto(LABEL199);
      }
      print("As you are dead, I can only permit you to: Restore Game,  Restart Game or Take Inventory. Sorry.");
    }
  }
LABEL199:
  return;

LOGIC__FOOTER

