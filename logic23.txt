#include "vars.txt"

LOGIC__HEADER



if (fROOM_SCRIPT_EXECUTED) {
  load.pic(vROOM_NO);
  draw.pic(vROOM_NO);
  discard.pic(vROOM_NO);
  set.horizon(46);
  load.logics(LOGIC_SWIMMING);
  call(LOGIC_SWIMMING);
  if (fGOAT_COMPANION) {
    load.logics(LOGIC_GOAT);
  }
  if (vROOM_YOU_KILLED_THE_GOAT_IN == vROOM_NO) {
    add.to.pic(62, 0, 0, 108, 64, 0, 0);
  }
  LABEL41:
    if (vPREV_ROOM_NO == 26) {
      get.posn(oEGO, v60, v61);
      if (v60 < 50) {
        position(oEGO, 10, 50);
        goto(LABEL70);
      }
      position(oEGO, 116, 50);
    }
  LABEL70:
    ignore.blocks(oEGO);
    draw(oEGO);
    show.pic();
}
if (said("examine", "lake")) {
  print("You see the mountains and trees reflected in the mirror-like water.");
}
LABEL87:
  if (said("examine", "cattails")) {
    print("These cattails look soft and fuzzy.");
  }
LABEL99:
  if (!fEGO_ON_WATER) {
    if (posn(oEGO, 69, 76, 103, 131) || posn(oEGO, 104, 96, 123, 120) || posn(oEGO, 120, 96, 131, 105) || posn(oEGO, 120, 106, 125, 111)) {
      set(fEGO_ON_WATER);
      set(f134);
    }
  }
LABEL140:
  if (v94 <= 0 && fEGO_ON_WATER) {
    v76 = 255;
    if (posn(oEGO, 108, 88, 140, 110) || posn(oEGO, 104, 111, 125, 120)) {
      v76 = 114;
      v77 = 102;
    }
    if (posn(oEGO, 86, 125, 106, 156)) {
      v76 = 73;
      v77 = 139;
    }
    LABEL193:
      if (v76 == 255) {
        v76 = 0;
        goto(LABEL218);
      }
    LABEL206:
      get.posn(oEGO, v60, v61);
      v76 -= v60;
      v77 -= v61;
      set(f134);
  }
LABEL218:
  if (!fBUCKET_FILLED && !fEGO_ON_WATER && !f116) {
    if (said("drink", "water") || said("get", "drink")) {
      if (posn(oEGO, 0, 40, 109, 167) || posn(oEGO, 110, 55, 151, 142)) {
        print("Ahhh!  That water sure hit the spot!");
        goto(LABEL274);
      }
      print("You are not close enough to the water.");
    }
  }
LABEL274:
  if (!fBUCKET_FILLED && fCARRYING_BUCKET && !fEGO_ON_WATER && said("get", "water")) {
    if (posn(oEGO, 0, 40, 109, 167) || posn(oEGO, 110, 55, 151, 142)) {
      reset(f116);
      print("The clear mountain water pours into the bucket.");
      get("water");
      set(fBUCKET_FILLED);
      if (!fFILL_BUCKET_POINTS) {
        vSCORE += 2;
      }
      set(fFILL_BUCKET_POINTS);
      goto(LABEL335);
    }
    print("You are not close enough to the water.");
  }
LABEL335:
  if (vEGO_EDGE_CODE != 0) {
    if (f66) {
      reset(f66);
      reset(fGOAT_COMPANION);
      set(fGOAT_IS_GONE);
    }
  }
LABEL355:
  if (vEGO_EDGE_CODE == 1) {
    new.room(26);
  }
LABEL364:
  if (vEGO_EDGE_CODE == 2) {
    new.room(22);
  }
LABEL373:
  if (vEGO_EDGE_CODE == 4) {
    new.room(24);
  }
LABEL382:
  if (vEGO_EDGE_CODE == 3) {
    new.room(10);
  }
LABEL391:
  if (fGOAT_COMPANION) {
    call(LOGIC_GOAT);
  }
LABEL399:
  call(LOGIC_SWIMMING);
  return;

LOGIC__FOOTER

