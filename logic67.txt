#include "vars.txt"

LOGIC__HEADER



if (fROOM_SCRIPT_EXECUTED) {
  load.pic(vROOM_NO);
  draw.pic(vROOM_NO);
  discard.pic(vROOM_NO);
  load.view(105);
  load.sound(5);
  set.horizon(40);
  if (vPREV_ROOM_NO == 66) {
    position(o0, 9, 152);
    set.loop(o0, 0);
    move.obj(o0, 9, 152, 0, f148);
  }
  if (vPREV_ROOM_NO == 68) {
    position(o0, 120, 41);
    set.loop(o0, 1);
    move.obj(o0, 120, 41, 0, f148);
  }
  LABEL58:
    draw(o0);
    show.pic();
}
if (said("examine", "stairway") || said("examine", "room")) {
  print(null);
}
LABEL81:
  if (fEGO_TOUCHED_TRIGGER_LINE && !f21) {
    set(f21);
    if (!f79) {
      set.view(o0, 105);
    }
    start.cycling(o0);
    set(f98);
    get.posn(o0, v60, v61);
    get.priority(o0, v30);
    set.priority.v(o0, v30);
    print();
    v61 = 165;
    v96 = 4;
    move.obj.v(o0, v60, v61, 96, f20);
    sound(5, f159);
  }
LABEL133:
  if (f20) {
    reset(f20);
    erase(o0);
    set(f63);
  }
LABEL145:
  if (vEGO_EDGE_CODE == 1) {
    new.room(68);
  }
LABEL154:
  if (posn(o0, 5, 150, 6, 167)) {
    new.room(66);
  }
LABEL166:
  return;

LOGIC__FOOTER

