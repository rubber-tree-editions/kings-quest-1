#include "vars.txt"

LOGIC__HEADER

if (fROOM_SCRIPT_EXECUTED) {
  load.pic(vROOM_NO);
  draw.pic(vROOM_NO);
  discard.pic(vROOM_NO);
  load.view(105);
  load.sound(SND_FALLING);
  set.horizon(40);
  if (vPREV_ROOM_NO == 66) {
    position(oEGO, 9, 152);
    set.loop(oEGO, 0);
    move.obj(oEGO, 9, 152, 0, f148);
  }
  if (vPREV_ROOM_NO == 68) {
    position(oEGO, 120, 41);
    set.loop(oEGO, 1);
    move.obj(oEGO, 120, 41, 0, f148);
  }
  draw(oEGO);
  show.pic();
}
if (said("examine", "stairway") || said("examine", "room")) {
  print("The stairs are moss-covered and slippery.");
}
if (fEGO_TOUCHED_TRIGGER_LINE && !f21) {
  set(f21);
  if (!fEGO_INVISIBLE) {
    set.view(oEGO, 105);
  }
  start.cycling(oEGO);
  set(f98);
  get.posn(oEGO, vTEMP_X, vTEMP_Y);
  get.priority(oEGO, v30);
  set.priority.v(oEGO, v30);
  print("Darn!  You slipped on the steep stairs.");
  vTEMP_Y = 165;
  v96 = 4;
  move.obj.v(oEGO, vTEMP_X, vTEMP_Y, 96, f20);
  sound(SND_FALLING, fSOUND_END_MARKER);
}
if (f20) {
  reset(f20);
  erase(oEGO);
  set(fGAME_OVER);
}
if (vEGO_EDGE_CODE == EDGE_HORIZON) {
  new.room(68);
}
if (posn(oEGO, 5, 150, 6, 167)) {
  new.room(66);
}
return;

LOGIC__FOOTER

