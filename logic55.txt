#include "vars.txt"

LOGIC__HEADER



if (fROOM_SCRIPT_EXECUTED) {
  load.pic(vROOM_NO);
  draw.pic(vROOM_NO);
  discard.pic(vROOM_NO);
  set.horizon(70);
  if (vPREV_ROOM_NO == 54) {
    position(oEGO, 79, 71);
  }
  if (vPREV_ROOM_NO == 2) {
    position(oEGO, 75, 164);
  }
  LABEL36:
    draw(oEGO);
    show.pic();
}
if (said("examine", "chandelier") || said("examine", "hall") || said("examine", "building")) {
  print(null);
}
LABEL65:
  if (said("talk", "knight")) {
    print("There is nobody in the armor.");
  }
LABEL77:
  if (said("examine", "knight")) {
    print("This is the armor of one of King Edward's knights from years ago.");
  }
LABEL89:
  if (said("get", "knight")) {
    print();
  }
LABEL101:
  if (vEGO_EDGE_CODE == 1) {
    new.room(54);
  }
LABEL110:
  if (posn(oEGO, 0, 166, 159, 167)) {
    new.room(2);
  }
LABEL122:
  return;

LOGIC__FOOTER

